page { background: #f5f6fa; color: #202438; font-family: -apple-system, BlinkMacSystemFont, "PingFang SC", sans-serif; }
button { margin: 0; padding: 0; background: transparent; font-size: inherit; line-height: inherit; border-radius: 0; }
button::after { border: none; }
.page { max-width: 960rpx; margin: 0 auto; padding: 28rpx 32rpx calc(40rpx + env(safe-area-inset-bottom)); box-sizing: border-box; }
.page-heading { font-size: 44rpx; font-weight: 700; letter-spacing: -1rpx; line-height: 1.35; }
.page-caption { margin-top: 10rpx; font-size: 26rpx; color: #777e92; line-height: 1.6; }
.btn-primary, .btn-ghost { width: 100% !important; min-height: 88rpx; padding: 22rpx 20rpx; box-sizing: border-box; line-height: 44rpx; text-align: center; border-radius: 22rpx; font-size: 28rpx; font-weight: 600; }
.btn-primary { background: #6255e7; color: #fff; }
.btn-primary[disabled] { opacity: .45; }
.btn-ghost { background: #eceafa; color: #5549c7; }
.btn-danger { color: #c54d69; }
.empty { padding: 80rpx 28rpx; text-align: center; color: #777e92; font-size: 28rpx; line-height: 1.8; }

.empty-art { display: block; width: 340rpx; height: 280rpx; margin: 0 auto 20rpx; }
